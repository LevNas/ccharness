---
name: session-wrap
description: セッション終了の合図（終わります・お疲れさま等）や作業の区切りで実行する締め処理。進捗更新 → issue 更新 → ナレッジ記録 → コミットの順。該当なしはスキップ可だがスキップを報告する。
---

# session-wrap — セッション終了時の定例

## トリガー

セッション終了の合図（「終わります」「お疲れさま」など）があれば、明示指示がなくても本手順を実施してからコミットします。作業の明確な区切りでも実行してかまいません。

## 手順（順に実施）

1. **進捗更新**: 進行中のプランやタスクの状態を更新します（`.claude/tasks/` やプロジェクトの進捗ファイル）。
2. **issue 更新**: 関連する issue にコメント、ラベル付与、クローズを行います。該当 issue が無ければスキップ可。必要なら新規起票を提案します。
3. **ナレッジ記録**: 設計判断・落とし穴・課題解決・指摘された誤りを記録します（ccmemo が入っていれば `/record-knowledge`）。
4. **コミット**: 変更をコミットします。push は明示指示がない限り行いません。コミットメッセージの言語はリポジトリの規約に従います。

## スキップ規則

- 該当のないステップはスキップできます。
- ただしスキップした旨を必ず報告します。
